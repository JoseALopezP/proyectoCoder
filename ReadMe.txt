1) Agregado archivo ReadMe
2) Busco automatizar los valores repetitivos dentro de las clases del css. Para esto:
 a)Genero un nuevo archivo donde trabajar "_mixinBase"
 a)Agrego un mixin con la lista de propiedades mas comunes de los bloques.
 b)Agrego un mapa para agregar el nombre de las clases a editar (clases que comparten esas caracteristicas).
 c)Agrego mixin a 2 clases base (.container y .mapBlock) donde voy a agregar las otras clases con @extend
 c)Realizo un @Each que recorra el mapa de clases realizando los extend diferenciando con un condicional para identificar a qué clase adherirlo.
3)Agregué SEO (keywords, description, favicon y title) a todas las páginas
